apiVersion: v1
kind: Service
metadata:
  name: model-webhook-service
spec:
  selector:
    app: model-webhook
  ports:
    - name: control
      protocol: TCP
      port: 12000
      targetPort: 12000
  type: ClusterIP
